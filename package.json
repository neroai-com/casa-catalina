{
  "name": "catalina-rentals",
  "private": true,
  "description": "Catalina Rentals — property landing pages + central booking/content CMS (no build steps, no runtime deps)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node dev-server.mjs",
    "build": "npx turbo run build",
    "test:e2e": "echo 'see /tmp e2e suites run via python3'"
  }
}
